query getAllGenes($first: Int = 1000, $after: String) {
  genes(first: $first, after: $after) {
    nodes {
      name
      conceptId
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
